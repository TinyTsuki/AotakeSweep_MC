{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${version}",
  "name": "${mod_name}",
  "description": "${mod_desc}",
  "authors": [
    "${mod_authors}"
  ],
  "contact": {
    "sources": "${sources_url}",
    "issues": "${issues_url}"
  },
  "license": "${license}",
  "icon": "pack.png",
  "environment": "*",
  "entrypoints": {
    "main": [
      "xin.vanilla.aotake.internal.fabric.FabricAotakeEntry"
    ],
    "client": [
      "xin.vanilla.aotake.internal.fabric.client.FabricAotakeClientEntry"
    ]
  },
  "mixins": [
    "${mod_id}.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${loader_version}",
    "fabric": "*",
    "minecraft": "${minecraft_version}",
    "banira_codex": "${banira_version_range}"
  }
}
